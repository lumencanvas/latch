import { defineModel } from '../../defineModel'

export default defineModel({
  id: 'Xenova/gte-small',
  name: 'GTE Small (384-dim)',
  family: 'transformers',
  task: 'feature-extraction',
  size: '~130 MB',
  license: 'mit',
})
