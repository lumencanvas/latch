# Deep Audit — Modernization Phases 0–2A (2026-06-14)

Adversarial review of the `modernization` branch (7 commits at audit time) before
implementing the Phase 2 dirty-flag engine. Method: re-read every changed file,
trace edge cases, run the full suite / typecheck / lint / web build, and read the
executors that the dirty-flag rewrite will classify. Findings below are split
into **fixed**, **latent (pre-existing) bugs found**, **design notes for Step B**,
and **outstanding/can't-verify-headless**.

## Verification run (all green)
- Unit suite **1126 passing / 11 todo (1137)**; typecheck 0 errors; eslint clean
  (only the expected `vite.config.ts` ignore warning).
- `npm run build:web` succeeds and ships `dist/web/coi-serviceworker.min.js` with
  the `index.html` reference intact.
- Golden snapshots stable across re-runs, no obsolete snapshots.

## Fixed this audit
- **Render-loop concurrency race (Phase 1).** `scheduleLoop()` re-armed
  `requestAnimationFrame` *after* `await executeFrame()` with no state recheck. If
  the tab was hidden (or stop/pause fired) during that async gap and then resumed,
  a stale loop and the new loop could both run → frames at 2×. Fixed with a
  monotonic `loopToken`: a loop re-arms only while it's still active, and
  stop/pause/hidden call `invalidateLoop()`. Regression test added (fails on the
  pre-fix code). Committed in `b2c589d`.

## Verified safe (claims I'd previously asserted)
- **Electron is unaffected by the web service worker.** `electron.vite.config.ts`
  builds the renderer from `src/renderer/index.html` and `src/main/index.ts` does
  `loadFile('../renderer/index.html')`. The coi-serviceworker `<script>` lives only
  in the *root* `index.html` (web/Vite entry), so there's no SW reload-loop risk in
  the desktop app. It also self-gates on `isSecureContext` + `crossOriginIsolated`.
- **COEP `credentialless` is the correct choice.** The app loads cross-origin,
  no-cors subresources (Google Fonts CSS/woff2 via `<link>`, HF model weights and
  MediaPipe WASM via fetch). `require-corp` would block those unless each sends
  CORP; `credentialless` lets them load (credentials stripped) while still enabling
  `crossOriginIsolated`. Safari (no credentialless) degrades to non-isolated /
  single-threaded — still functional.

## Latent (pre-existing) bugs found — NOT introduced by this work
- **`smooth` node is a silent no-op.** `smoothExecutor` reads `_prev` from controls
  and writes `_prev` as an *output*, but the engine's `handleSpecialOutputs` never
  feeds `_prev` back into controls (it only handles `_dynamicInputs/_dynamicControls/`
  `_dynamicOutputs/_preset_code/_controlUpdates`). So `prev` is always `undefined`,
  `prev ?? target = target`, and `smoothed = target + (target-target)*… = target`
  every frame — i.e. **smoothing does nothing; output == input**, regardless of the
  `factor`/`deltaTime`. The code comment even says "this would need special handling
  in the engine." Fix later by routing `_prev` through `_controlUpdates`, or persist
  state in a module Map like the other stateful nodes. (Consequence: golden flow #5
  "stateful smoothing converges" is mislabeled — it currently just passes through.)

## Design notes carried into Phase 2 Step B
- **Purity must be verified by reading, not grepping.** Grepping executors for
  `getState`/`Math.random`/`ctx.totalTime`/etc. is insufficient: `gate` has zero
  such markers yet is stateful via a module-level `gateLastValue` Map. Therefore
  `data.ts`/`string.ts` showing "0 markers" does **not** prove purity. The
  dirty-flag pure-set is built **only from directly-read executors**.
- **Verified PURE node types (safe to skip when inputs+controls unchanged):**
  `constant, add, subtract, multiply, divide, map-range, clamp, abs, trig, power,
  vector-math, modulo, lerp, step, smoothstep, remap, quantize, wrap, compare,
  and, or, not, select, switch` (24 types). Everything else defaults to
  **always-run** (safe-by-default — a misclassification can never freeze a node;
  worst case is no speedup). Notable exclusions and why: `gate` (module state),
  `smooth` (uses `ctx.deltaTime`), `random` (`Math.random`), `start`/`trigger`/
  `interval`/`delay`/`timer`/`metronome`/`step-sequencer`/`console`/`monitor`/
  `oscilloscope`/`equalizer` (module-level state), `time`/`lfo` (time-dependent).
- **Equivalence-harness state leak.** Executor state is module-global keyed by
  `nodeId`. Running the same flow full-then-dirty would let the first run's state
  bleed into the second for module-stateful nodes. Mitigation: the equivalence
  harness namespaces node IDs per run (distinct prefixes) so state can't collide.
  (The current golden flows happen to use no module-stateful nodes, but the
  equivalence harness should not depend on that.)
- **Dirty mode must reuse `executeNode`** (which also runs `handleSpecialOutputs`
  + metrics) so executed nodes behave identically to full mode; only the *decision
  to skip* is new. Change detection = `firstRun || controlsChanged ||
  anyUpstreamChangedThisFrame`. "Changed" is computed conservatively: primitives by
  `===`; any object/array output ⇒ treated as changed (never wrongly skipped).

## Async-nonblocking: blast-radius finding (drove the design)
Reading the executors before implementing "async-nonblocking" revealed that a
blanket engine-level change would be **harmful**, not helpful:
- **36 executors are `async`.** Most are *per-frame* nodes — MediaPipe
  hand/face/pose/object/segmentation/gesture/audio, webcam, audio-input, clasp
  video receive. They `await` their result **every frame by design** (detect →
  draw that frame). Fire-and-latching them would make results land a frame late
  and the in-flight guard would *drop detections* — degrading the AI/vision
  features.
- The genuinely frame-stalling case is **long, occasionally-triggered I/O**:
  HTTP `await fetch` (seconds), heavy generative inference. `httpExecutor` already
  returns cached values when not triggered; the stall is the `await fetch` on
  trigger, which freezes the whole rAF loop until it resolves.
- **Therefore:** implemented an **opt-in `deferredNodeTypes` set** (default empty
  ⇒ zero behavior change). Only opted-in types fire-and-latch; everything else
  (incl. all MediaPipe/webcam) keeps the awaited path byte-for-byte. The in-flight
  guard checks *before* calling the executor, so the async work is not re-fired
  while pending (no request storm). Out-of-band results are dropped after `stop()`
  and propagated to downstream pure nodes in dirty mode via `pendingAsyncChange`.
- **Enablement is deferred:** turning this on for real types (`http`/`http-request`
  and heavy generative AI) needs in-app validation (downstream consumers, the
  `loading` output, one-frame-later semantics). MediaPipe/webcam/audio-input must
  never be added to the deferred set.

## Outstanding / cannot verify headless
- **Headers runtime check.** `self.crossOriginIsolated === true` and AI/MediaPipe
  nodes still loading must be confirmed once in a real browser (`npm run dev`,
  check the `[LATCH] crossOriginIsolated=…` console log). Not verifiable in the
  test environment.

## Follow-up audit (deferred/dirty engine + Phase 3a)
- **Deferred/dirty engine re-reviewed — clean.** Default path is byte-identical
  (executor called once, awaited); both opt-in branches are gated by an
  empty-by-default set. The in-flight guard correctly precedes the executor call
  (a bug caught earlier by a failing test), so deferred work never re-fires while
  pending. `npm run build:web` passes; full suite green; golden snapshots
  unchanged. Minor accepted notes: `inFlightAsync` isn't GC'd when a node is
  removed mid-flight (cleared by the promise's `.finally` or by `stop()`);
  non-native thenables wouldn't be deferred (real executors return native
  Promises); the dirty `executed` count includes a deferred node that returned
  cached (diagnostic only).
- **Vue Flow was already upgraded in `node_modules`.** `package.json` declared
  `^1.33.5`/`^1.3.0`/`^1.1.1`/`^1.4.0`, but the installed (and lockfile) versions
  are already `core 1.48.2 / background 1.3.2 / controls 1.1.3 / minimap 1.5.4` —
  the carets resolved to latest at a prior install, so the app already builds/runs
  on 1.48.2. Phase 3a therefore just makes `package.json` truthful + enables the
  perf flag. The "auto-generated handle IDs removed (default null)" 1.4x breaking
  change does **not** affect LATCH: handles render with explicit `:id="input.id"`/
  `:id="output.id"` (`BaseNode.vue:416/446`), and the engine requires explicit
  `sourceHandle`/`targetHandle` on edges.

## Phase 3d — transformers.js v3 → v4 (verified compatible)
- Bumped `3.8.1 → 4.2.0`. Verified the app's API surface against the **installed
  type defs** (not assumptions): `env.allowLocalModels`, `env.useBrowserCache`
  still exist (v4 adds `env.useWasmCache`); `pipeline`/`env` still exported.
  **Typecheck passes with zero code changes**; `npm run build:web` succeeds
  (Vite resolves the browser export, so the new `sharp` Node dep isn't bundled);
  full suite green (no test imports transformers).
- **Runtime-validation gap (in-browser only):** the worker accesses pipeline
  options via `any` (`(pipeline as any)`, `Record<string,any>`), so v4 changes to
  the *runtime* semantics of `device:'webgpu'`, `dtype`, the chat `messages`
  input, or `progress_callback` fields would not be caught by typecheck/build.
  Must confirm a model loads + runs in a real browser before relying on it.
- **Heavy dep tree:** v4 pulls `@huggingface/tokenizers`, `@huggingface/jinja`, a
  **nightly `onnxruntime-web`** (`1.26.0-dev…`), and native `sharp`. `npm install`
  reported 52 advisories (mostly transitive sharp/dev-ORT) — review later; do NOT
  `npm audit fix --force` (would break the pinned ORT/transformers). v4 unlocks
  Phase 4 (q1/q2 dtypes, refreshed model catalog, WebGPU runtime).

## Minor / accepted
- `nodeById` Map keeps the *last* entry on duplicate node IDs, vs `Array.find`'s
  *first*. Duplicate IDs are invalid in Vue Flow; accepted.
- `start()` while status is `paused` schedules a loop that immediately returns
  (store stays paused). Pre-existing guard behavior; the real path is `toggle()` →
  `resume()`. Harmless; accepted.

## Phase 4 deep audit — Vector Memory + catalog refresh (2026-06-14, later same day)
Adversarial review of the two newest pieces of Phase 4 (the `Vector Memory` node
and the `AI_MODELS` catalog refresh), by reading the actual code paths. Method as
before: read every touched path, trace edge cases, run suite/typecheck/lint/build.

### Bug found + fixed: new chat models prompted as raw completion models
- The AI worker (`ai.worker.ts` `generateText`) chooses the **messages/chat**
  format vs a **plain completion** prompt by matching the repo-id against
  substrings (`chat|instruct|llama|-it-|phi-`). The catalog refresh added
  `onnx-community/Qwen3-0.6B-ONNX`, `Qwen3-1.7B-ONNX`, and the abliterated Qwen3 —
  **none contain any of those substrings**, so they would have been prompted as
  raw completion models and produced garbage (no chat template applied).
  Qwen2.5-**Instruct**, SmolLM2-**Instruct**, Llama, gemma-**it**, Phi-4-mini all
  matched correctly; only the base-named Qwen3 entries broke.
- **Fix:** extracted the detection into a pure module `services/ai/textGenFormat.ts`
  (`isChatModel`), added `qwen` to the match set, and had the worker import it.
  Added `tests/unit/services/text-gen-format.test.ts` (3) including a **contract
  test** asserting every text-generation entry in `AI_MODELS` is detected as chat —
  so a future catalog addition that isn't chat-detected fails CI instead of
  silently degrading output. This is the catalog↔worker invariant made explicit.

### Verified safe (read, not assumed)
- **No dangling references to removed model ids.** Grepped the renderer +
  tests for `mobilenet_v3_large`, `all-mpnet-base-v2`, `Phi-3-mini-4k`: none
  outside the catalog. `getDefaultModel` resolves from `AI_MODELS` by task and the
  defaults were left unchanged, so no default points at a removed id. The worker's
  "TinyLlama-Chat" comment is still valid (TinyLlama remains the text-gen default).
- **Schema change is non-breaking.** The only consumer of `ModelDefinition`/
  `ModelOption` is `AIModelManagerModal.vue`, which reads `id/name/size/
  supportsWebGPU` — never `license`. Adding required `license`/`defaultLicense`
  type-checks (only `AI_MODELS` constructs these) and doesn't touch the UI.
- **Vector Memory (re-confirmed).** Stable corpus reference is correct under dirty
  mode (`outputsDiffer` is `Object.is`; every mutation rebuilds the snapshot →
  new ref → flagged; unchanged frames keep the ref → Retrieve correctly skipped).
  Real embeddings are plain `number[]` (worker `Array.from(output.data)`), so the
  `Array.isArray` guard accepts live model output. Engine cleanup wired:
  `gcRAGState` in `updateGraph`'s GC block (line ~193), `disposeAllRAGState` in
  `stop()` (line ~813). Per-node `disposeVectorMemoryNode` mirrors its (uncalled)
  sibling helpers — consistent, not a leak.

### Codebase-analysis note
- `docs/architecture/ARCHITECTURE.md` (v1.0, 2026-01-17) is an **aspirational
  design doc**, not a live map: it shows `src/nodes/*Node.ts` class executors and
  an `src/engine/graph` layout that the real code does not use (the real structure
  is `registry/<cat>/` definitions + `engine/executors/<cat>.ts` function
  executors, per CLAUDE.md). New-subsystem docs were therefore added to the
  accurate `docs/nodes/ai.md` (Vector Memory + Retrieve) rather than to the stale
  design doc. Treat ARCHITECTURE.md as intent, CLAUDE.md + `docs/nodes/*` as truth.

### Verification (all green)
- Suite **1194 passing / 11 todo** (1191 → +3 chat-format); typecheck 0 errors;
  eslint clean on changed files; `npm run build:web` succeeds (worker bundles the
  new `textGenFormat` import).

## Phase 4 deep audit — WebLLM concurrency (2026-06-15)
- **Bug found + fixed: concurrent generations on the shared engine.** `WebLLMService`
  holds one engine; two LLM nodes triggering near-simultaneously each `await
  ensureEngine()` then both call `chat.completions.create` on the same engine, and
  the first node's `interrupt()` was a no-op because its stream loop hadn't started
  yet — so two streams interleave and stale tokens from a superseded generation
  land in state. Fixed with the **monotonic `genToken`** pattern (same idiom as the
  Phase 1 render-loop race): every `startGeneration` bumps the token and checks it
  after each await / each streamed chunk, bailing if superseded; a new generation
  settles the previous active node to `done` and calls `interruptGenerate()`;
  `interrupt()` and `disposeAll()` also bump the token so in-flight loops bail.
  Regression test added (a gated mock stream proves a superseded generation's late
  tokens are dropped). Suite 1224 → 1225.
- **Known remaining limitation (documented, low-risk):** `ensureEngine` itself
  isn't serialized, so two *simultaneous model-switch* triggers could still race to
  create/dispose engines (leaking one). The token guard stops the stale generation
  from streaming; the engine-create race is rare (needs concurrent different-model
  triggers) and is browser-validated territory — left as a follow-up.

## Browser validation (2026-06-15, Playwright + system Chrome)
Drove headless system Chrome (`channel:'chrome'`) against `npm run dev`:
- ✅ `self.crossOriginIsolated === true` (SharedArrayBuffer available) — COOP/COEP
  headers served (`same-origin`/`credentialless`, confirmed via `curl -I`) and
  honored; the Phase-0 `[LATCH] crossOriginIsolated=true` console log fires.
  **Resolves the long-standing headers acceptance item.**
- ✅ App mounts (title "LATCH", #app populated).
- ✅ WebGPU adapter present (`navigator.gpu.requestAdapter()` → adapter; Chrome→Metal
  on macOS). WebLLM/transformers WebGPU paths can run here; the remaining gap for
  full AI validation is the multi-GB model download, not code.

### transformers.js v4 model load — validated end-to-end (2026-06-15)
Network egress confirmed (HF/jsdelivr 200). In the crossOriginIsolated dev page,
imported `@huggingface/transformers@4.2.0` and ran a real pipeline:
`feature-extraction` on `Xenova/all-MiniLM-L6-v2` → **downloaded the model and
returned a correct 384-dim embedding** (`[-0.0946, 0.0084, -0.0143, …]`), no page
errors, threaded WASM (crossOriginIsolated). **Resolves the transformers.js v4
runtime acceptance item** — the worker uses the same `pipeline`/`env` API. (WebGPU
*device* for transformers + the full WebLLM model stream remain manual — they need
GB-scale downloads; the runtime API + isolation are proven.)

### clasp 3.3.2 → 4.3.2 (Phase 3c) — drop-in, verified
Diffed the used surface (`Clasp`/`ClaspBuilder`/`Value`) against v3 tag vs v4
source + npm: identical (only additive `browser` export + optional `set()` 3rd
arg). Bumped; typecheck + build + suite green, zero code changes. Realtime
send/receive against a live CLASP server is the only remaining runtime check.

### clasp v4 export-resolution audit (2026-06-15)
v4 added a `browser` export (`dist/index.global.js`, an IIFE). Checked the concern
that Vite might bundle the IIFE global → named imports resolve `undefined`. The
exports order is `{ types, import, require, browser }` — `import` precedes
`browser`, so a bundler matching `import` picks the ESM build. Confirmed via node
ESM: `import {Clasp, ClaspBuilder}` → both `function`. No footgun; bump is sound.

### three r162 → r184 (Phase 3b) — render-validated
Research (migration guide) showed the big breaks predate our r162 baseline; grep of
our code found none of the r163+ break patterns. Bumped three + @types/three to
0.184; only 6 type-only casts needed (stricter `@types/three` 0.184). Browser
render-validation (Playwright + system Chrome): an r184 `WebGLRenderer` +
`ShaderMaterial` GLSL fixture returned the exact shader color `[255,0,0,255]` via
`readRenderTargetPixels` on a WebGL2 context, and the app mounted with zero console
errors. Per-shader-node visual regression remains a heavier (graph-driving) check.
**Internal-property audit (2026-06-15):** our texture bridge reads three's internal
`renderer.properties.get(tex).__webglTexture` (TextureBridge.ts, ThreeRenderer.ts) —
a cast makes it compile but couldn't catch an internal rename. Verified in Chrome at
r184: `properties.get(rt.texture)` → keys `[__webglTexture, __version, __renderTarget]`
and `__webglTexture instanceof WebGLTexture` is true. The internal survives r184, so
the Three↔Pixi bridge works at runtime.

### Phase 5 UI finding (capability-duality) — why the BaseNode badge is deferred
Wiring `getCapabilityStatus` into `BaseNode.vue` (1165 lines) for platform-gated
nodes needs a per-platform requirement model, NOT a single capability key: serial/
midi/ble work via the **web API on web OR the native path on Electron**, so a naive
`requiresCapability: 'webSerial'` would show a false "unavailable" badge in Electron
(where the node works). Only `webgpu` (LLM) and `camera` (webcam) are
platform-agnostic. Deferred so the requirement model is designed correctly rather
than shipping misleading warnings into the central node component.
